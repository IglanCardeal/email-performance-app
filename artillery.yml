config:
  target: "http://localhost:3000"
  phases:
    - duration: 1
      arrivalRate: 16
scenarios:
  - name: "Chamada para o banco para exibir historico de emails"
    flow:
      - get:
          url: "/stress"
